[project]
name = "analysis-core"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "data-streams",
    "ros-python-conversions",
    "opencv-python>=4.5.0",
    "tqdm>=4.60.0",
]

[tool.setuptools.packages.find]
where = ["src"]

