# visualization

Helpers for **2D visualization** of maps, grid layers, and related image outputs.

Built on **`geometry`** (and NumPy). Use for quick debugging and figures over gridmap-backed data rather than as a full GUI stack.
